# 要求 5：光学自动控制联调

## 赛题约束

- 信号源只连接示波器 X 轴，与控制装置之间不得有任何电气耦合。
- LEFT、MID、RIGHT 分别一键启动对角线、圆和“∞”形自动模式。
- 控制时间不超过 10 s。
- 正确图形的漂移和抖动小于 1 div 后，应继续稳定至少 5 s。
- 测试频率为 1 kHz～100 kHz，步进 100 Hz，不能预先写入装置。

## 当前光学链路

- TSL1401 AO：PA14 / ADC0 channel 12
- TSL1401 CLK：PA17
- TSL1401 SI：PA18
- PA15：DAC 锯齿波输出，用于把屏幕纵向位置映射为时间
- AD9959 CH0：识别频率后产生最终 Y 轴信号

线阵应竖直安装并穿过示波器屏幕中心。频率探测时，PA15 产生纵向锯齿，
线阵记录 X=0 处的过零点序列；最终图形阶段，线阵记录目标曲线与 X=0
测试线的交点。

## 三种目标的正确交点

- 对角线：一个位于屏幕中心的紧凑亮点。
- 圆：两个关于屏幕中心对称、间距尽可能大的亮点。
- “∞”形：一个位于屏幕中心的紧凑自交点。

## 当前开发分支修正

- 输入线断开时，LEFT/MID/RIGHT 直接进入三个光学自动模式。
- 输入线存在且电锁相有效时，保留要求 1～4 的原按键行为。
- “∞”形评分由错误的“双峰对称”改为“单峰居中”。
- 频率估计由整数亮点计数改为亮点间距计算，消除窗口边缘的周期取整误差。
- 锯齿窗口按常见命中顺序重排，优先覆盖低频、中频和高频，降低最坏搜索时间。

## 上板后的最小验证顺序

1. 确认信号源只接示波器 CH1，装置输入 PA16 完全断开。
2. 烧录开发分支固件，串口执行 `ccd status`。
3. 执行 `ccd capture 5 45`，确认线阵有足够对比度且屏幕中心约在像素 63～64。
4. 执行 `ccd freq`，分别测试 1.0、1.1、2.0、5.0、17.0、50.0、90.0、100.0 kHz。
5. 记录每次的 `CCD_PROBE`、`CCD_FREQ complete` 和总耗时。
6. 依次按 LEFT、MID、RIGHT，验证三个模式均无需串口命令即可启动。
7. 对每种模式记录最终频率、相位、控制时间，并连续观察至少 5 s。

## 暂未验证

- PA15 每个新锯齿窗口仍保留重复启动和固定等待；确认首帧启动行为后再缩短。
- 声光完成提示目前只有 RGB 状态灯，蜂鸣器硬件和引脚尚未确认。

## 2026-07-30 实测记录

### 1.0 kHz 光学测频

- CCD 中心基准峰：像素 63，峰宽 6，跨度 902。
- 曝光时间：40 ms。
- 选中锯齿窗口：5 ms。
- 可见规则过零点：9 个，几何推算为 10 个。
- 规则点跨度：64 像素。
- 光学粗测：1000 Hz。
- 最终识别：1000 Hz。
- 总识别时间：2497 ms。
- DDS 候选细搜关闭，电锁相关闭。

实时调整 TSL1401 时运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass `
  -File .\tools\ccd_live_view.ps1 -Port COM3
```

黄色曲线显示全部 128 个实际 ADC 值，红圈标出固件识别的峰，青色虚线为
像素中心 63.5。关闭窗口后才会释放 COM3 给其他串口测试命令。
