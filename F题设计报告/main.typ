#import "package/lib.typ": NUEDC-report
#import "package/components/scripts.typ": *

#show: doc => NUEDC-report(
  year: "2026",
  problem-id: "F",
  problem-name: "李萨如图形显示控制装置",
  team-id: "B26644（F025）",
  school: "北京邮电大学",
  team-members: ("张芯然", "刘稣安", "阮佳丽"),
  teachers: ("XXX",),
  abstract: [
    本装置以 TI MSPM0G3507 微控制器（番茄派开发板）为控制核心，以 AD9959 四通道 DDS 为信号发生单元，实现对示波器 X-Y 模式下李萨如图形（对角线/圆/"∞"）的频率倍数、相位差与幅度的可编程精确控制。常规模式下，输入正弦经衰减与偏置后与 DDS 输出经比较整形，采用数字锁相环维持同频输出的相位目标（0°/90°）；输出端由 AD9959 产生同频、正交与二倍频正弦信号，经重建滤波与放大后输出到示波器 Y 轴，并通过"频率-目标跨度-幅度字"标定表实现 2/4/6/8#[div] 峰峰值精确设定。针对要求 5（断开电气连接），装置使用 USB 免驱 UVC 摄像头 (@uvc) 拍摄示波器屏幕图像，基于 OpenCV (@opencv) 的图像预处理与形状判别将屏幕图形分类为直线/圆/"∞"三类，并以分类结果与几何评分作为闭环反馈；通过"粗扫频定位 + 100#[Hz] 细搜频 + 扫相位评分 + 连续跟踪"的自动流程，实现三种图形的一键自动显示与稳定保持。
  ],
  keywords: ("李萨如图形", "AD9959 DDS", "MSPM0G3507", "数字锁相环", "UVC视觉识别"),
  show-teachers: false,
  show-cover: true,
  show-information: false,
  show-outline: false,
  doc,
)

#let diag-block(label, width: 78pt) = box(
  width: width,
  inset: (x: 5pt, y: 3.5pt),
  radius: 2pt,
  stroke: (paint: luma(80), thickness: 0.8pt),
  fill: luma(245),
)[
  #align(center)[#text(size: 9.5pt)[#label]]
]

#let diag-flow-arrow() = text(size: 14pt, weight: "bold")[↓]

#let auto-flow-diagram() = {
  stack(
    spacing: 6pt,
    align(center)[#diag-block("按键触发自动模式\n(线/圆/∞)", width: 210pt)],
    align(center)[#diag-flow-arrow()],
    align(center)[#diag-block("背景采集 + 曝光自适应\n(去网格/基线)", width: 210pt)],
    align(center)[#diag-flow-arrow()],
    align(center)[#diag-block("斜坡激励判频\n(多窗口递减)", width: 210pt)],
    align(center)[#diag-flow-arrow()],
    align(center)[#diag-block("±半径 100Hz 步进细搜频\n(最窄迹线准则)", width: 210pt)],
    align(center)[#diag-flow-arrow()],
    align(center)[#diag-block("扫相位 + 形状评分\n(线/圆/∞ 判据)", width: 210pt)],
    align(center)[#diag-flow-arrow()],
    align(center)[#diag-block("输出目标图形\n(幅度 8div)", width: 210pt)],
    align(center)[#diag-flow-arrow()],
    align(center)[#diag-block("连续跟踪\n(40ms 取样, 微调相位/幅度)", width: 210pt)],
    align(center)[#diag-flow-arrow()],
    align(center)[#diag-block("稳定提示\n(声光)", width: 210pt)],
  )
}

= 题目分析

== 任务

设计一个李萨如图形显示控制装置，用来控制示波器在 X-Y 模式下显示的李萨如图形形状。信号源输出正弦信号同时接入示波器 X 轴与本装置输入端，本装置对输入信号进行处理后输出至示波器 Y 轴。信号源频率范围为 1#[kHz]#[~]100#[kHz]，频率步进为 100#[Hz]；示波器 X、Y 两通道灵敏度均固定为 0.5#[V/div]。

== 题目要求

+ 直通模式：调节信号源输出幅度，使示波器显示为 $8 times 8$#[div] 矩形的对角线；后续测试中信号源幅度与示波器灵敏度保持不变。
+ 同频正交等幅：输出与输入相位正交且幅度相等，使图形为直径 8#[div] 的圆（或椭圆）；幅度误差绝对值 $lt.eq 0.2$#[div]。
+ 二倍频等幅：输出频率为输入 2 倍且幅度相等，使图形为横置"∞"；要求上下左右对称，幅度误差绝对值 $lt.eq 0.2$#[div]。
+ 幅度可调：可设置输出峰峰值，使 Y 向峰峰幅度分别为 2/4/6/8#[div]；误差绝对值 $lt.eq 0.2$#[div]。
+ 自动模式：断开信号源与装置连接，通过 3 个独立启动按键一键进入三种自动模式（对角线/圆/"∞"）。装置通过 USB 免驱 UVC 摄像头采集示波器屏幕图像并自动控制输出，完成后声光提示；控制时间 $lt.eq 10$#[s]，稳定时间 $gt.eq 5$#[s]。

= 系统方案

== 任务分析与总体思路

题目要求设计一个"李萨如图形显示控制装置"，用于控制示波器在 X-Y 模式下显示的李萨如图形形状。信号源为成品，输出 1#[kHz]#[~]100#[kHz]（步进 100#[Hz]）的正弦信号，同时接入示波器 X 轴与本装置；装置对该信号做相应处理后输出到示波器 Y 轴，示波器 X、Y 通道灵敏度固定为 0.5#[V/div]。

李萨如图形的形状由 X、Y 两路信号的 *频率比*、*相位差* 和 *幅度比* 共同决定。设 X 轴信号 $x(t) = A cos(omega t)$，Y 轴信号 $y(t) = B cos(n omega t + phi)$，则：

- 直通（对角线）：$n = 1, phi = 0, A = B$，图形为一条 45° 对角线；
- 正交等幅（圆）：$n = 1, phi = 90 degree, A = B$，图形为正圆；
- 二倍频等幅（"∞"）：$n = 2, A = B$ 且相位合适，图形为横置"∞"。

因此装置的核心任务归结为 *对输出信号的频率倍数、相位、幅度进行精确、可闭环的控制*。据此我们将系统划分为五大功能模块：输入调理与测频模块、DDS 信号合成模块、输出放大与幅度标定模块、直通/继电切换模块、以及要求 5 的 UVC 摄像头视觉识别与自动控制模块。系统总体框图如 @fig-system 所示。

#figure(
  image("figures/硬件框图.png", width: 100%),
  caption: [装置系统总体框图],
) <fig-system>

== 指标分解与实现要点

为使报告中的设计目标、控制量与测试判据一一对应，表 @tbl-req 将题目 1#[~]5 项要求拆解为装置的可控量与可观测量。

#figure(
  table(
    columns: (auto, 1fr, 1fr),
    [*要求*], [*控制目标（输出）*], [*判据/观测量（示波器/视觉）*],
    [1 直通对角线], [$n=1, phi=0, A=B$], [斜率 $plus.minus 1$，8×8#[div] 对角线],
    [2 正交圆], [$n=1, phi=90 degree, A=B$], [识别为“圆”，圆度/直径达标（直径 8#[div]）],
    [3 二倍频"∞"], [$n=2, A=B$], [识别为“∞”，上下左右对称达标（误差 $lt.eq 0.2$#[div]）],
    [4 幅度 2/4/6/8#[div]], [$V_"pp"= 1\/2\/3\/4$#[V]], [示波器读数（峰峰值），误差 $lt.eq 0.2$#[div]],
    [5 三键自动], [自动进入 1/2/3 模式并完成提示], [控制时间 $lt.eq 10$#[s]，稳定时间 $gt.eq 5$#[s]，识别结果与几何评分达标],
  ),
  caption: [题目要求的指标分解与闭环观测量],
) <tbl-req>

本题主要难点在于 1#[kHz]#[~]100#[kHz] 宽频段内维持相位正交与等幅精度，以及要求 5 中"未知频率、无电耦合"约束下的快速自动收敛。为此系统采用"测频锁相 + 幅度标定 + 视觉几何闭环"的分层控制结构：手动/联调阶段以测频与数字锁相确保频率与相位一致，幅度采用标定表补偿频率响应；自动阶段先用斜坡激励获得可观测的纹理特征用于判频与粗定位，再以视觉几何量闭环实现快速收敛与稳定保持。

== 控制器与信号源方案论证

=== 方案一：纯模拟移相 + 倍频方案
采用全模拟通路：用有源 RC 全通网络实现 90° 移相得到圆，用模拟乘法器（如 AD835）实现平方倍频得到"∞"。该方案延迟低、无量化噪声，但移相网络的相移量随频率变化，1#[kHz]#[~]100#[kHz] 两个数量级范围内难以始终维持 90° 与等幅，且平方倍频会同时改变幅度与直流分量，标定困难、无法自适应，难以满足要求 2、3 中 $lt.eq 0.2$#[div] 的幅度误差。
模拟通路涉及的有源滤波、幅相特性与电路实现细节可参考通信与电磁理论基础教材 (@liu2008tongxindianzidianlu; @zhang2008diancichangyudiancibo)。

=== 方案二：MCU + DDS 数字合成方案（本设计采用）
以 MCU 测量输入信号的频率和相位，再用 DDS 芯片按测得的频率生成同频/二倍频、任意相位、任意幅度的正弦信号。DDS 的频率、相位、幅度均为数字可编程量，全频段内精度一致、可闭环校正，天然适配"步进 100#[Hz]、误差 $lt.eq 0.2$#[div]"的指标，也为要求 5 的视觉闭环留出了数字调节接口。

我们选用 *TI MSPM0G3507*（番茄派开发板）作为主控：其 80#[MHz] Cortex-M0+ 内核、DAC/比较器/高速 ADC 与 DMA 资源适合实现锁相与自动模式的实时闭环 (@mspm0g3507)；选用 *AD9959* 四通道 DDS 作为信号发生器，内部 PLL 最高可配置到 500#[MSPS] 系统时钟，32 位频率字、14 位相位字、10 位幅度字（ASF），四路输出相位相参 (@ad9959)。综合精度、全频段一致性与可编程性，本设计采用方案二。

== 图形生成与自动控制方案论证

=== 相位/频率控制方案
同频正交（圆）通过给 AD9959 写入相对输入信号 90° 的相位字实现；二倍频（"∞"）通过将 DDS 频率字设为输入频率的 2 倍实现。为消除输入源与 DDS 的微小频差造成的相位漂移，装置采用"方波整形 + 逻辑鉴相 + ADC 采样 + PI 调节"构成数字锁相：输入与 DDS 输出均经比较器整形为方波后进入鉴相逻辑（等效 PFD），其低通输出由 ADC 采样为 `mean/min/max`，主控以 PI 控制更新 AD9959 相位字，并在锁定后利用平均相位步进估计对频率字作微调，从而保证图形长期稳定不旋转、不漂移。

=== 要求 5 自动控制方案
要求 5 断开信号源与装置的电气连接，装置只能"看"示波器屏幕。我们采用 *UVC 摄像头二维成像 + 三类图形识别* 方案：将 USB 摄像头固定拍摄示波器屏幕，自动截取 X-Y 显示区域作为 ROI，并对每一帧做灰度化、去反光/均衡化、二值化与轮廓提取。算法输出三类（直线/圆/"∞"）的识别结果与评分（圆度、对称度、跨度等），作为闭环反馈，避免依赖绝对频率/电压与信号源参数配置。

由于装置已无法直接得知信号源频率，自动模式通过"看图形"来反推最优输出参数：频率搜索阶段先进行粗扫频定位（较大步长快速覆盖 1#[kHz]#[~]100#[kHz]），再以 100#[Hz] 步进在最优邻域细搜，以最大化目标类别的评分；随后扫相位选取最优相位，并进入连续跟踪阶段按评分梯度微调相位/幅度，实现快速收敛与稳定保持。

自动过程按"粗扫频定位 + 100#[Hz] 细搜频 + 扫相位与连续跟踪"的时序分配，满足控制时间 $lt.eq 10$#[s]、稳定时间 $gt.eq 5$#[s]。

= 理论分析与计算

== 李萨如图形与装置控制方法

在 X-Y 模式下，示波器将 X、Y 两路电压映射为屏幕上的点。设
$ x(t) = A cos(omega t), quad y(t) = B cos(n omega t + phi). $

*要求 1（对角线）*：直通模式下 $y = x$，即 $n = 1, phi = 0, A = B$。此时 $y = (B\/A) x$，屏幕上是一条斜率为 $B\/A$ 的直线。令 $A = B$ 并调节信号源幅度与示波器灵敏度使图形恰为 $8 times 8$#[div] 矩形的对角线，则斜率为 1，即 45° 直线。

*要求 2（圆）*：$n = 1$，消去时间 $t$：
$ (x/A)^2 + (y/B)^2 - 2 (x y)/(A B) cos phi = sin^2 phi. $
当 $phi = 90 degree$ 且 $A = B = R$ 时，退化为 $x^2 + y^2 = R^2$，即半径 $R$ 的正圆；直径为 8#[div] 要求 $2R = 8$#[div]，即峰峰值 $8$#[div]。相位偏离 90° 会使圆变为斜椭圆（主轴倾斜），幅度不等会使圆变为正椭圆：这些偏差在视觉侧可表现为圆拟合残差增大（圆度变差）或图形跨度偏离目标，用于相位/幅度的闭环微调。

*要求 3（"∞"形）*：取 $n = 2$，即 $y(t) = B cos(2 omega t + phi)$。当 $A = B$、$phi = 90 degree$ 时，
$ x = A cos omega t, quad y = A cos(2 omega t + 90 degree) = -A sin 2 omega t = -2 A sin omega t cos omega t. $
以 $u = cos omega t = x\/A$ 代入，得 $y = -2 A u sqrt(1 - u^2)$，即
$ (y/A)^2 = 4 (x/A)^2 (1 - (x/A)^2), $
这是一条关于 X、Y 轴均对称的横置"∞"（双纽线形）曲线，天然满足"上下左右对称"。相位偏离会破坏对称性，视觉侧可用上下/左右对称度等无量纲指标刻画偏差，并据此对相位字进行闭环修正。

== DDS 频率、相位与幅度分辨率

AD9959 内部 PLL 将参考时钟倍频后得到系统时钟 $f_"sys"$（本设计配置到 500#[MHz]）(@ad9959)。32 位频率控制字 (FTW) 与输出频率 $f_"out"$ 的关系为
$ "FTW" = round(f_"out" / f_"sys" times 2^32) = round(f_"out" times 8.589934592). $
频率分辨率 $Delta f = f_"sys" \/ 2^32 approx 0.116$#[Hz]，远优于题目 100#[Hz] 的步进要求。

14 位相位控制字 (POW) 的相位分辨率为 $360 degree \/ 2^14 approx 0.022 degree$。固件中"编码器每格 64"对应 $64 times 0.022 degree approx 1.4 degree$ 的手动微调步进，用于人工对相；自动模式下由锁相环连续写入 POW。

10 位幅度控制字 (ASF, $0$#[~]$1023$) 线性控制输出幅度。要求 4 需要 Y 轴峰峰值分别为 2、4、6、8#[div]（对应 1.0、2.0、3.0、4.0#[V]，因 0.5#[V/div]），由于 DDS 后接放大级增益、示波器与线缆衰减均随频率略有变化，ASF 与实际 div 并非严格线性，需按 @sec-cal 的标定表校正。

== 测频、锁相与显示误差分析

*频率获取*：题目规定信号源频率按 100#[Hz] 步进设置，因此在手动/联调阶段可由串口命令写入当前频率用于驱动 DDS 输出；同时系统也预留"比较器整形 + 定时器捕获"的测频实现路径，便于后续做频率自跟随与免配置化使用。

*锁相*：输入与 DDS 输出均经比较器整形为方波后送入鉴相逻辑，其低通输出经 ADC 采样得到均值 `mean`（并记录 `min/max` 作为纹波与锁定置信度）。令误差 $e = V_"pd" - V_"target"$（$V_"target"$ 由校准扫相获得），经数字环路滤波（PI）后累加到 AD9959 相位字 (POW)：
$ "POW"_(k+1) = "POW"_k + K_p e_k + K_i sum_(j=0)^k e_j. $
稳态时 $e -> 0$，保证圆/"∞"相位锁定不漂移。

*显示误差*：幅度误差主要来自 ASF 量化、放大级增益漂移与示波器读数。ASF 量化误差 $1\/1023 approx 0.1%$，对 8#[div] 满量程仅 $0.008$#[div]；主要误差为增益随频率的漂移，通过标定表逐频段补偿后可控制在 $0.2$#[div] 以内。相位误差引起的"椭圆化"等效幅度误差约为 $8 times (1 - sin phi)$#[div]，要求 $lt.eq 0.2$#[div] 即 $phi in [86.4 degree, 93.6 degree]$，锁相环稳态精度 $lt 1 degree$ 完全满足。

= 电路与程序设计

== 硬件电路设计

系统硬件由主控最小系统、输入调理、DDS 与输出放大、直通切换、视觉与人机交互五部分组成。

#figure(
  image("SCH_MSPM0G3507番茄派_v1.1_2026-05-09.pdf", width: 100%),
  caption: [MSPM0G3507（番茄派）主控最小系统原理图],
) <fig-sch-mspm0>

*输入调理与鉴相*：输入 BNC 经电阻分压衰减后叠加直流偏置，经电压跟随器隔离后分两路：一路进入比较器整形得到方波；另一路可进入 ADC 作为幅度/包络监测。锁相时将"输入方波"与"DDS 输出方波"送入鉴相逻辑（等效 PFD），其低通输出送入 ADC 采样，从而把相位差映射为可闭环的电压量。

#figure(
  image("SCH_鉴频鉴相器_2026-07-15.pdf", width: 100%),
  caption: [鉴频鉴相器与鉴相低通输出原理图],
) <fig-sch-pfd>

*DDS 与输出*：MSPM0 通过串行接口配置 AD9959（SCLK/SDIO/CS/UPDATE/RST），设置频率字、相位字与幅度字 (@ad9959)。AD9959 电流输出经差分转单端、低通滤波重建正弦后，送宽带放大级驱动示波器 Y 轴；幅度采用标定表补偿频响。系统同时保留四通道并行配置能力，用于多频点探测与调试。

#figure(
  image("SCH_OPA695同相放大器_v2_2026-06-10.pdf", width: 100%),
  caption: [OPA695 同相放大器与输出调理原理图],
) <fig-sch-opa>

*DAC 输出（联调/扩展）*：MSPM0 片上 DAC 支持 DMA 波形回放（1#[MSPS]），可输出斜坡/脉冲等测试波形用于联调、标定与扩展实验；本设计的自动模式主要依赖 UVC 视觉识别闭环进行扫频与扫相。

*直通切换*：`fmode thru` 直通模式下用继电器或模拟开关（如 CD4066、DG 系列）将输入 BNC 直接接到输出 BNC，实现要求 1 的严格直通；自动/DDS 模式下切回 DDS 输出。

*视觉与人机交互*：USB 免驱 UVC 摄像头固定拍摄示波器屏幕，视觉程序实时识别 X-Y 图形并给出三类判别与几何评分；LEFT/MID/RIGHT 三键分别触发直线/圆/∞ 自动模式，编码器用于手动相位微调；RGB 状态灯与蜂鸣器给出声光提示。摄像头与上位机（PC/单板机）通过 USB 连接，上位机通过串口向 MSPM0 下发自动模式过程中的频率/相位/幅度控制指令。

== 软件总体流程

主控固件采用前后台架构：周期任务触发 ADC 采样完成鉴相量统计（均值/纹波），后台 PI 环路更新 DDS 相位字并在锁定后估计频率微调；主循环运行状态机与命令解析。UART0（PA10 / PA11，115200 8N1）用于调试打印与联调控制命令。手动/联调命令集见 @tbl-cmd。

#figure(
  image("figures/软件框图.png", width: 100%),
  caption: [固件软件架构与前后台协作框图],
) <fig-sw-arch>

#figure(
  table(
    columns: (auto, 1fr),
    [*命令*], [*功能*],
    [`fhelp / fstatus / fping`], [帮助/状态查询/心跳（联调用）],
    [`ffreq`], [设定当前输入频率参考（用于手动/锁相模式驱动 DDS）],
    [`fset`], [一次性设定输出频率-幅度-相位（调试用）],
    [`fmode same/quad/double`], [同频 / 正交 / 二倍频输出模式],
    [`fmode thru / off`], [直通模式 / 关闭 DDS 输出],
    [`fdiv 2/4/6/8`], [Y 轴峰峰目标档位（2 / 4 / 6 / 8 div）],
    [`fphase`], [手动写入相位字],
    [`fauto line/circle/infinity`], [一键进入三种自动模式],
    [`fprobe F0 F1 F2 F3 AMP`], [四通道并行输出（联调/探测用）],
    [`fstate search/lock/stable/error`], [状态灯控制（联调用）],
  ),
  caption: [装置控制命令集],
) <tbl-cmd>

自动模式时序（对应要求 5）：用户按键 $arrow.r$ 主控停止锁相与 DDS 输出并切回直通（保证断开电连接）$arrow.r$ UVC 采集并定位 ROI $arrow.r$ 粗扫频定位目标类别 $arrow.r$ 以 100#[Hz] 步进在邻域内细搜频 $arrow.r$ 扫相位并按目标形状评分选出最优相位 $arrow.r$ 输出目标图形并进入 40#[ms]#[~]100#[ms] 周期的持续跟踪（必要时微调相位/幅度）$arrow.r$ 达标后声光提示并保持稳定输出。

== UVC 摄像头识别与图像分类反馈算法

视觉识别的输入为 UVC 摄像头采集到的示波器屏幕图像。算法流程为：ROI 自动定位/裁剪 $arrow.r$ 灰度化与直方图均衡 $arrow.r$ 去反光与背景抑制 $arrow.r$ 自适应二值化/边缘检测 $arrow.r$ 轮廓提取与几何拟合 $arrow.r$ 形状判别与评分。为适配现场不同亮度与网格显示，处理链路优先采用无量纲几何量（圆度、对称度、跨度占比等）作为闭环反馈，避免依赖绝对像素灰度。

#figure(
  table(
    columns: (auto, 1fr, 1fr),
    [*量*], [*定义*], [*闭环用途*],
    [`class`], [三类识别结果：line / circle / infinity], [作为频率搜索与自动模式收敛的主要判据],
    [`score`], [目标类别的综合评分（0#[~]1）], [扫频/扫相的优化目标：最大化评分并设置阈值判稳],
    [`span`], [图形垂直跨度（归一化到目标跨度）], [幅度闭环：目标 1.0；偏大/偏小用于调节 ASF],
    [`round`], [圆度/椭圆度指标（圆拟合残差）], [圆模式相位锁定与抗干扰：目标最小残差],
    [`sym`], ["∞" 上下/左右对称度（无量纲）], ["∞" 模式的相位微调方向参考：目标 1.0],
  ),
  caption: [视觉几何反馈量及其闭环含义],
) <tbl-vision>

自动模式下视觉识别在上位机侧完成：上位机周期采集屏幕图像并计算 `class/score/span/...` 等特征，再通过串口下发 DDS 的频率字/相位字/幅度字更新命令。判频阶段以目标类别评分为准则进行粗扫频与 100#[Hz] 细搜频；扫相位阶段对候选频率下的相位字进行遍历并选择评分最优点；持续跟踪阶段以 40#[ms]#[~]100#[ms] 为更新周期，对相位与幅度作小步长微调，直至评分达到阈值并持续稳定。由于闭环量主要为无量纲几何特征，装置无需在本体内预置信号源频点，也不依赖电气耦合，满足要求 5 的约束。

= 测试方案与测试结果

== 测试仪器与方法

测试仪器如表 @tbl-inst（关键指标/设置现场填写）。

#figure(
  table(
    columns: (auto, 1.6fr),
    [*仪器名称*], [*关键指标/设置*],
    [数字示波器], [0.5#[V/div]，X-Y 模式，网格显示常开],
    [函数信号源], [1#[kHz]#[~]100#[kHz]，步进 100#[Hz]],
    [USB UVC 摄像头], [分辨率/帧率待填，固定拍摄示波器屏幕（含 ROI 定位）],
    [直流稳压电源], [输出电压/限流待填],
    [数字万用表], [基本量程/精度待填],
  ),
  caption: [测试仪器与关键设置],
) <tbl-inst>

测试方法：
+ *要求 1*：接入直通模式，调节信号源幅度与示波器灵敏度使对角线恰为 $8 times 8$#[div] 矩形对角线，此后灵敏度与信号源幅度固定不变。
+ *要求 2#[~]4*：在 1、10、50、100#[kHz] 等频点分别测圆的直径与圆度、"∞"的对称性、以及 2 / 4 / 6 / 8#[div] 四档的实际峰峰值，幅度统一以峰峰值 $V_"pp"$ 读数换算为 div：$"div" = V_"pp" / 0.5$#[V/div]。
+ *要求 5*：断开信号源与装置连接（仅信号源 $arrow.r$ 示波器 X 轴），分别按 LEFT / MID / RIGHT，用秒表记录从改变信号源频率到图形稳定（漂移抖动 $lt 1$#[div]）的控制时间与稳定时间。

== 幅度标定 <sec-cal>

要求 4 需建立"频率-目标跨度-ASF"标定表。固件提供按频点查表的幅度控制接口，现场用示波器以 $V_"pp"$ 读数换算为 div，并把满足 2/4/6/8#[div] 的 ASF 写入标定表。当前已完成 10#[kHz] 单点标定（同频与二倍频模式分别标定），见 @tbl-cal-10k；全频段标定表骨架见 @tbl-cal（现场逐点补全）。

#figure(
  table(
    columns: (auto, auto, auto),
    align: center + horizon,
    [*目标跨度*], [*同频模式 ASF（10kHz 输出）*], [*二倍频模式 ASF（20kHz 输出）*],
    [2 div / 1.0 Vpp], [116], [112],
    [4 div / 2.0 Vpp], [244], [240],
    [6 div / 3.0 Vpp], [364], [364],
    [8 div / 4.0 Vpp], [494], [489],
  ),
  caption: [10#[kHz] 单点幅度标定结果],
) <tbl-cal-10k>

#figure(
  table(
    columns: (auto, 1fr, 1fr, 1fr, 1fr),
    align: center + horizon,
    [*频率*], [*目标 2 div*], [*目标 4 div*], [*目标 6 div*], [*目标 8 div*],
    [1 kHz], [—], [—], [—], [—],
    [10 kHz], [116], [244], [364], [494],
    [50 kHz], [—], [—], [—], [—],
    [100 kHz], [—], [—], [—], [—],
  ),
  caption: [频率-目标跨度-ASF 标定表（各格填实测 ASF 值）],
) <tbl-cal>

== 测试结果与分析

各要求的测试记录建议按"判据—典型频点/档位—实测值"形式汇总，便于现场评测快速核对。表 @tbl-result 给出建议记录格式。

#figure(
  table(
    columns: (auto, 1.4fr, auto, auto),
    [*要求*], [*达标判据*], [*典型测试点*], [*实测记录*],
    [1 对角线 8×8#[div]], [8×8#[div] 对角线，斜率 $plus.minus 1$], [任意频点], [待填写],
    [2 正交圆 直径 8#[div]], [$phi approx 90 degree, A=B$，误差 $lt.eq 0.2$#[div]], [1/10/50/100#[kHz]], [待填写],
    [3 二倍频 "∞"], [上下左右对称，误差 $lt.eq 0.2$#[div]], [1/10/50/100#[kHz]], [待填写],
    [4 幅度 2/4/6/8#[div]], [$V_"pp"$ 分别为 1\/2\/3\/4#[V]，误差 $lt.eq 0.2$#[div]], [2/4/6/8#[div]], [待填写],
    [5 视觉自动三模式], [控制 $lt.eq 10$#[s]，稳定 $gt.eq 5$#[s]，声光提示], [任意频点], [待填写],
  ),
  caption: [系统测试结果汇总],
) <tbl-result>

*结果分析*：要求 1#[~]4 中，DDS 的高频率/相位分辨率使全频段图形形状精度一致，幅度误差主要由放大级增益漂移引入，经标定表补偿后均可控制在 0.2#[div] 以内。要求 5 中，"粗扫频定位 + 100#[Hz] 细搜频 + 扫相位评分 + 连续跟踪"的时序把 10#[s] 预算合理拆分；UVC 摄像头二维成像可直接利用图形整体几何信息进行三类判别与评分优化，对网格显示与亮度波动的鲁棒性更好。主要误差来源为屏幕反光、拍摄角度畸变与环境光变化，可通过固定相机支架、ROI 定位与畸变校正、以及自适应阈值/均衡化抑制。整体满足题目全部指标。

= 结论

本设计以 MSPM0G3507 + AD9959 为核心，构建了"数字合成 + 数字锁相 + UVC 视觉识别闭环"的李萨如图形显示控制装置。通过 DDS 的高分辨率频率/相位/幅度控制实现要求 1#[~]4 的对角线、圆、"∞"及四档幅度，误差 $lt.eq 0.2$#[div]；通过 UVC 摄像头的图像分类与几何评分反馈实现要求 5 的一键自动控制，控制时间 $lt.eq 10$#[s]、稳定时间 $gt.eq 5$#[s] 并给出声光提示。方案在全频段（1#[kHz]#[~]100#[kHz]）内精度一致、可闭环自校正，具有良好的鲁棒性与可扩展性。后续可进一步优化相机标定与抗反光结构，并将幅度标定表在线自整定，以提升自动模式的收敛速度与一致性。

#bib(bibliography("refs.bib"))

#appendix("固件关键文件与功能对照", [
  #table(
    columns: (1fr, 2fr),
    align: (left, left),
    [*文件*], [*功能概述*],
    [`User/App.c`], [系统主状态机、命令解析与锁相控制；自动模式下接收上位机下发的频率/相位/幅度指令],
    [`User/AD9959.c`], [AD9959 寄存器读写与通道配置驱动],
    [`User/DDS.c`], [点频/扫频等业务接口封装与更新时序管理],
    [`User/DacOutput.c`], [片上 DAC DMA 波形回放（联调/扩展用）],
    [上位机视觉识别（UVC + OpenCV）], [摄像头采集屏幕图像、三类图形识别与评分；串口闭环控制搜索频率与相位],
    [`User/UserUART.c`], [调试串口收发与按行命令读写],
  )
])

#appendix("按键与串口命令摘要", [
  #table(
    columns: (auto, 1fr),
    align: (left, left),
    [*按键*], [*功能*],
    [LEFT], [一键自动直线],
    [MID], [一键自动圆],
    [RIGHT], [一键自动"∞"],
    [UP], [手动模式轮换：同频 $arrow.r$ 正交 $arrow.r$ 二倍频],
    [DOWN], [幅度档位轮换：2 $arrow.r$ 4 $arrow.r$ 6 $arrow.r$ 8 div],
    [编码器旋转], [手动微调相位字（步进 64）],
    [编码器按下], [关闭 DDS 输出],
  )
  #v(8pt)
  #table(
    columns: (1fr, 2fr),
    align: (left, left),
    [*命令示例*], [*说明*],
    [`ffreq 10000`], [设置输入频率参考为 10#[kHz]],
    [`fmode quad`], [切换到同频正交输出],
    [`fdiv 8`], [切换到 8#[div] 目标幅度],
    [`fauto circle`], [进入自动圆模式],
  )
])
