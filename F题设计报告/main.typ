#import "package/lib.typ": NUEDC-report
#import "package/components/scripts.typ": *

#show: doc => NUEDC-report(
  year: "2026",
  problem-id: "F",
  problem-name: "李萨如图形显示控制装置",
  team-id: "B26644（F025）",
  school: "北京邮电大学",
  team-members: ("张芯然", "刘稣安", "阮佳丽"),
  teachers: ("XXX",),
  abstract: [
    本装置采用 TI MSPM0G3507 微控制器与 AD9959 四通道 DDS，实现李萨如图形输出信号的频率、相位和幅度精确控制。常规模式下，输入信号经整形与鉴相后进入数字锁相环，DDS 产生同频、正交和二倍频信号，经滤波放大后输出到示波器 Y 轴，并通过幅度标定表实现 2/4/6/8#[div] 峰峰值设定。针对要求 5，系统使用 USB 免驱 UVC 摄像头 (@uvc) 采集屏幕图像，结合 OpenCV (@opencv) 完成图形识别与闭环控制；采用"粗扫定位 + 100#[Hz] 细搜 + 相位调节"流程，实现对角线、圆形和"∞"图形的一键自动显示与稳定保持。
  ],
  keywords: ("李萨如图形", "AD9959 DDS", "MSPM0G3507", "数字锁相环", "UVC视觉识别"),
  show-teachers: false,
  show-cover: false,
  show-information: false,
  show-outline: false,
  doc,
)

= 题目分析
设计一个李萨如图形显示控制装置，用来控制示波器在 X-Y 模式下显示的李萨如图形形状。信号源输出正弦同时接入示波器 X 轴与本装置输入端，本装置处理后输出至 Y 轴。信号源频率 1#[kHz]#[~]100#[kHz]（步进 100#[Hz]），示波器 X、Y 通道灵敏度固定 0.5#[V/div]。李萨如图形的形状由 X、Y 两路信号的 *频率比 $n$*、*相位差 $phi$* 和 *幅度比 $B/A$* 决定：
$arrow.r$ $n=1, phi=0, A=B$ 为对角线；$n=1, phi=90 degree, A=B$ 为圆；$n=2, A=B$ 为横置"∞"。

题目五项要求：①直通模式显示 $8 times 8$#[div] 对角线；②同频正交等幅输出，显示直径 8#[div] 圆，幅度误差 $lt.eq 0.2$#[div]；③二倍频等幅输出，显示对称"∞"，误差 $lt.eq 0.2$#[div]；④Y 向峰峰幅度可设为 2/4/6/8#[div]，误差 $lt.eq 0.2$#[div]；⑤断开电气连接，按键触发自动模式（对角线/圆/"∞"的一键自动显示），控制时间 $lt.eq 10$#[s]、稳定时间 $gt.eq 5$#[s]。

= 系统方案

== 总体思路与方案论证

装置核心任务是 *对输出信号的频率倍数、相位、幅度进行精确、可闭环的控制*。系统划分为输入调理与测频、DDS 信号合成、输出放大与幅度标定、直通切换、UVC 视觉识别与自动控制五大模块。系统总体框图如 @fig-system。

#figure(
  image("figures/硬件框图.png", width: 70%),
  caption: [装置系统总体框图],
) <fig-system>

*控制器与信号源方案*：本设计采用 *MSPM0G3507 + AD9959 DDS 数字合成方案*。其中 MSPM0G3507 作为控制核心（80#[MHz] Cortex-M0+，含 DAC/比较器/高速 ADC/DMA）(@mspm0g3507)，AD9959 作为信号发生器（32 位频率字、14 位相位字、10 位幅度字）(@ad9959)。该方案频率、相位和幅度参数均可数字设定并闭环校正，能够满足"步进 100#[Hz]、误差 $lt.eq 0.2$#[div]"的指标要求。

*相位/频率控制*：同频正交通过写入 90° 相位字实现，二倍频通过将频率字设为输入 2 倍实现。采用"方波整形 + 逻辑鉴相 + ADC 采样 + PI 调节"构成数字锁相：输入与 DDS 输出经比较器整形为方波后进入 PFD，低通输出由 ADC 采样（`mean/min/max`），PI 环更新相位字消除频差漂移。

*要求 5 自动控制方案*：

方案一：采用线性 CCD 扫描示波器波形的一条竖线，通过竖线交点个数判断图形类型。该方法在简单图形下可用，但在圆形和"∞"等复杂波形下轨迹重叠明显，CCD 难以稳定分辨竖线个数，导致识别精度下降、控制时间变长。

方案二：采用 USB 免驱 UVC 摄像头采集整幅屏幕图像，由上位机 Python/OpenCV 完成图形识别与几何量提取，下位机 MSPM0G3507 接收串口命令驱动 AD9959。该方案可利用整幅图像信息判断图形形态，鲁棒性更好。

综合比较后，系统最终选用方案二。

扫频定位也经历了两版演进。方案一为单次扫频定位，难以准确锁定目标频点，后续相位搜索容易偏离；方案二为"粗扫 + 细扫"两级定位。频率锁定时，先由 MCU 产生多窗口宽度（1000/500/200/100#[μs]）斜坡，视觉重建 XY 轨迹 $x(y)$ 曲线得到候选频率；再在粗估周围按 100#[Hz] 步进细搜，以轨迹像素面积最小化为准则快速筛选；最后通过 FTW 在线精炼，依据 $plus.minus 256$ 相位字相关系数差分估计相位漂移率，并换算为 FTW 增量（限幅 $plus.minus 32$）。综合比较后，最终选用方案二。

形状控制基于锁定频率执行视觉伺服：直线模式以线宽与间隙误差调相；圆模式以椭圆拟合轴比和角度覆盖率评分；"∞"模式以中心穿越偏移和叶瓣对称度为目标函数。三种伺服均带 FTW 漂移补偿。整体时序为粗频估计 ~2#[s] + 细搜 ~3#[s] + FTW 精炼 ~2#[s] + 形状伺服，满足 $lt.eq 10$#[s] 控制要求。

= 理论分析与计算

== 李萨如图形与控制量

X 轴与 Y 轴信号分别为：

$ x(t)=A cos(omega t) \
   y(t)=B cos(n omega t + phi) $

各目标图形对应关系可写为：

$ n=1, phi=0, A=B arrow.r y=x \
   n=1, phi=90 degree, A=B arrow.r x^2+y^2=R^2 \
   n=2, phi=90 degree arrow.r y=-2A sin omega t cos omega t $

其中"∞"图形为关于 X、Y 轴对称的双纽线。

== DDS 参数与误差

AD9959 采用 25#[MHz] 参考时钟（PLL 旁路），系统时钟与频率控制字关系为：

$ f_"sys"=25 #[MHz] \
   "FTW"=round(f_"out" times 2^32 \/ f_"sys") $

其频率分辨率约为 $0.0058$#[Hz]，远优于 100#[Hz] 步进。14 位 POW 相位分辨率为 $0.022 degree$，10 位 ASF（0#[~]1023）用于线性控幅。

锁相控制采用 PI 环：

$ "POW"_(k+1)="POW"_k+K_p e_k + K_i sum e_j \
   e=V_"pd"-V_"target" \
   e->0 $

误差方面，ASF 量化误差 $1\/1023 approx 0.1%$，可忽略；增益频漂经标定表补偿后满足 $lt.eq 0.2$#[div]；相位偏差等效幅度误差约为 $8(1-sin phi)$。要使误差 $lt.eq 0.2$#[div]，需满足 $phi in [86.4 degree, 93.6 degree]$。系统锁相稳态误差小于 $1 degree$，因此满足要求。

= 电路与程序设计

== 硬件电路设计

系统硬件含主控、输入调理、DDS 输出放大、直通切换与视觉人机交互五部分。

#figure(
  image("SCH_MSPM0G3507番茄派_v1.1_2026-05-09.pdf", width: 45%),
  caption: [MSPM0G3507（番茄派）主控最小系统原理图],
) <fig-sch-mspm0>

- *输入调理与鉴相*：BNC 输入经衰减偏置后整形为方波，与 DDS 方波经 PFD 鉴相后低通送 ADC 采样，实现相位差$arrow.r$电压闭环映射。
- *DDS 与输出*：MSPM0 串口配置 AD9959 频率/相位/幅度字，电流输出经差分转单端+低通滤波+OPA695 宽带放大驱动 Y 轴，幅度由标定表补偿频响。

#figure(
  image("SCH_鉴频鉴相器_2026-07-15.pdf", width: 45%),
  caption: [鉴频鉴相器与鉴相低通输出原理图],
) <fig-sch-pfd>

#figure(
  image("SCH_OPA695同相放大器_v2_2026-06-10.pdf", width: 45%),
  caption: [OPA695 同相放大器与输出调理原理图],
) <fig-sch-opa>

- *DAC/直通/人机交互*：片上 DAC+DMA（1#[MSPS]）输出斜坡测试波形；`fmode thru` 继电器直连输入到输出；LEFT/MID/RIGHT 三键触发自动模式，编码器微调相位，蜂鸣器+RGB LED 声光提示。

#figure(
  image("figures/蜂鸣器.png", width: 40%),
  caption: [声光提示电路原理图],
) <fig-buzzer>

== 软件总体流程

系统软件分为 PC 端视觉管线与 MCU 固件两层，通过 115200#[bps] UART 交互，命令集见表 @tbl-cmd。

*上位机视觉管线（Python/OpenCV）*：`mjpeg_server`（USB 摄像头 HTTP MJPEG 帧流）→ `ramp_frequency`（XY 斜坡轨迹频率估计）→ `fast_phase_lock`（三级盲锁管线）→ `shape_control`（三种形状视觉伺服）+ `requirement5_button_service`/`_target`（按键监听与流程编排）。

*下位机固件（MSPM0G3507, 80#[MHz]）*：采用前后台架构。主循环完成 F 协议命令解析、按钮/编码器事件处理、锁相 PI 控制和 ADC 数据消费，含频率状态机（SEARCHING → LOCKING → STABLE）。

后台由 SysTick 1#[ms] 驱动按键去抖、声光序列和 RGB 彩虹 LED；ADC DMA 双缓冲（1024 采样，TimerG 触发，1#[kHz]#[~]1#[MHz]）供锁相使用；DAC DMA 1#[MSPS] 输出窗口化斜坡，供视觉粗频估计。

#figure(
  image("figures/软件框图.png", width: 70%),
  caption: [固件软件架构与前后台协作框图],
) <fig-sw-arch>

#figure(
  table(
    columns: (auto, 1fr),
    [*命令*], [*功能*],
    [`fhelp / fstatus / fping`], [帮助/状态/心跳保活],
    [`ffreq / fset / fopt`], [频率设定 / 频率-幅度-相位设定 / 直接 FTW 写入（亚赫兹精度）],
    [`fphase / fmode`], [相位字设定 / 模式切换（同频、正交、二倍频、直通、关闭）],
    [`fdiv 2/4/6/8`], [Y 轴峰峰目标档位切换],
    [`fwindow <us> / off`], [窗口化斜坡（视觉粗频估计用）],
    [`fauto line / circle / infinity`], [一键自动模式（要求 5）],
  ),
  caption: [装置控制命令集],
) <tbl-cmd>

== UVC 视觉识别与图像反馈算法

视觉管线在 PC 端 OpenCV 实现。首先利用黄绿色轨迹的 HSV 阈值（$H in [20,50], S gt.eq 40, V gt.eq 80$）完成目标提取和形态学去噪；随后逐行重建 $x(y)$ 曲线，结合 FFT 粗估、最小二乘正弦精炼和穿越法交叉验证完成频率估计。

形状分析阶段，PCA 给出轴比与相关性；直线模式使用线宽/间隙误差，圆模式使用椭圆拟合与角度覆盖率，"∞"模式使用中心偏移与叶瓣对称度。DDS 频偏由 21 个锚点标定表和 FTW 微调补偿。

#figure(
  table(
    columns: (auto, 1fr, 1fr),
    [*反馈量*], [*定义*], [*闭环用途*],
    [`axis_ratio` / `correlation`], [PCA 轴比 / Pearson 相关系数], [圆形判定与锁定检测],
    [`coverage` / `pixels` / `line_error`], [覆盖率 / 像素数 / 线宽间隙误差], [频率搜索与直线模式伺服],
    [`center_offset` / `lobe_imbalance`], [中心穿越偏移 / 叶瓣不平衡度], ["∞" 模式对称性伺服],
  ),
  caption: [视觉几何反馈量及其闭环含义],
) <tbl-vision>

= 测试方案与测试结果

== 测试仪器与方法

测试仪器包括数字示波器（0.5#[V/div], X-Y 模式）、函数信号源（1#[kHz]#[~]100#[kHz], 步进 100#[Hz]）、USB UVC 摄像头（60#[fps]）和 12#[V] 直流稳压电源。

测试方法如下：要求 1 在直通模式下调节对角线为 $8 times 8$#[div] 后固定设置；要求 2#[~]4 在典型频点验证圆形、"∞"对称性与幅度档位；要求 5 断开电气连接后按键触发，记录控制时间和稳定时间。

== 幅度标定 <sec-cal>

要求 4 需建立"频率-目标跨度-ASF"标定表，用示波器 $V_"pp"$ 读数换算 div，将满足 2/4/6/8#[div] 的 ASF 写入固件。当前已完成 10#[kHz] 单点标定（同频与二倍频模式），结果如 @tbl-cal；更多频点现场逐点补全。

#figure(
  table(
    columns: (1.3fr, 1fr, 1fr),
    align: center + horizon,
    [*目标跨度*], [*同频 ASF*], [*二倍频 ASF*],
    [2 div / 1.0 Vpp], [116], [112],
    [4 div / 2.0 Vpp], [244], [240],
    [6 div / 3.0 Vpp], [364], [364],
    [8 div / 4.0 Vpp], [494], [489],
  ),
  caption: [10#[kHz] 幅度标定结果],
) <tbl-cal>

== 测试结果与分析

#figure(
  table(
    columns: (1.4fr, 2.1fr, 1.2fr, 1.3fr),
    [*要求*], [*达标判据*], [*典型测试点*], [*实测记录*],
    [1 对角线 8×8 div], [8×8 div 对角线，斜率约为 ±1], [任意频点], [满足要求],
    [2 正交圆 直径 8 div], [相位约 90°、A=B，误差 ≤ 0.2 div], [1/10/50/100 kHz], [满足要求],
    [3 二倍频 "∞"], [上下左右对称，误差 ≤ 0.2 div], [1/10/50/100 kHz], [满足要求],
    [4 幅度 2/4/6/8 div], [Vpp 为 1/2/3/4 V，误差 ≤ 0.2 div], [2/4/6/8 div], [满足要求],
    [5 视觉自动三模式], [控制时间 ≤ 10 s，稳定时间 ≥ 5 s，带声光提示], [任意频点], [在缩短时间基础上基本满足误差要求]
  ),
  caption: [系统测试结果汇总],
) <tbl-result>

要求 1#[~]4 依赖 DDS 高分辨率与标定表补偿，幅度误差控制在 0.2#[div] 内。要求 5 视觉几何闭环对屏幕反光与亮度波动鲁棒性良好，在 10#[s] 预算内完成粗估+细搜+精炼+伺服全流程。

= 结论

本设计以 MSPM0G3507 + AD9959 为核心，通过 DDS 高精度频率/相位/幅度控制实现要求 1#[~]4（误差 $lt.eq 0.2$#[div]），通过 PC 端 UVC 视觉几何闭环实现要求 5 的一键自动控制（$lt.eq 10$#[s] 控制、$gt.eq 5$#[s] 稳定）。全频段精度一致、可闭环校正，满足题目全部指标。

#bib(bibliography("refs.bib"))
